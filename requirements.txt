# ==============================================================================
# requirements.txt — AgroConnect Namibia (Monorepo Aggregator)
# ------------------------------------------------------------------------------
# FILE ROLE:
#   Single entrypoint for platforms that expect a root requirements.txt
#   (e.g., Render / simple CI jobs).
#
# DESIGN:
#   - The real dependency sources of truth live per-service:
#       • backend/requirements.txt
#       • ai_service/requirements.txt
#   - This file simply points pip to the right service requirements to prevent
#     dependency drift and "works locally, fails on deploy" issues.
#
# DEPLOY NOTES:
#   - If you deploy ONLY the backend as the web service (common), keep ONLY the
#     backend line enabled.
#   - If you deploy backend + ai_service in the SAME Python environment (less
#     common), uncomment the ai_service line too.
# ==============================================================================

-r backend/requirements.txt
# -r ai_service/requirements.txt
