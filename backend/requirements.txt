# ==============================================================================
# backend/requirements.txt — AgroConnect Namibia Backend Dependencies
# ------------------------------------------------------------------------------
# FILE ROLE:
#   Reproducible Python environment for the Flask backend API.
#
# RESPONSIBILITIES THIS FILE SUPPORTS:
#   • REST API (Flask)
#   • PostgreSQL (SQLAlchemy + psycopg v3)
#   • Migrations (Flask-Migrate/Alembic)
#   • Auth (PyJWT + bcrypt)
#   • Realtime (Flask-SocketIO)
#   • Redis (caching / presence / background queues as applicable)
#   • Production server (Gunicorn)
#
# DESIGN NOTES:
#   • psycopg v3 is used (preferred over psycopg2).
#   • eventlet is required because gunicorn_config.py uses eventlet workers.
#   • Pin only where stability matters; allow safe patch updates for drivers.
# ==============================================================================


# ------------------------------------------------------------------------------
# Core Web Framework
# ------------------------------------------------------------------------------
Flask==3.0.3
Werkzeug==3.0.3
Flask-Cors==4.0.1


# ------------------------------------------------------------------------------
# Database & ORM
# ------------------------------------------------------------------------------
SQLAlchemy==2.0.32
Flask-SQLAlchemy==3.1.1

# Migrations
Flask-Migrate==4.1.0
alembic==1.13.1

# PostgreSQL driver (psycopg v3)
# NOTE: Keep a safe range for binary wheels on Windows/Linux.
psycopg[binary]>=3.2.2,<3.3


# ------------------------------------------------------------------------------
# Authentication & Security (Project standard)
# ------------------------------------------------------------------------------
PyJWT==2.9.0
bcrypt==4.2.0


# ------------------------------------------------------------------------------
# Realtime / WebSockets + Redis
# ------------------------------------------------------------------------------
Flask-SocketIO==5.3.6

# REQUIRED when using eventlet worker class in Gunicorn / SocketIO
eventlet==0.36.1

# Redis client
redis==5.0.8


# ------------------------------------------------------------------------------
# Environment & Typing
# ------------------------------------------------------------------------------
python-dotenv==1.0.1
typing-extensions==4.12.2


# ------------------------------------------------------------------------------
# Utilities (common backend needs)
# ------------------------------------------------------------------------------
requests==2.32.3
email-validator==2.1.0.post1


# ------------------------------------------------------------------------------
# Production Server
# ------------------------------------------------------------------------------
gunicorn==22.0.0
