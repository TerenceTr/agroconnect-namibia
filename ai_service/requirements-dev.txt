# ==============================================================================
# ai_service/requirements-dev.txt — Dev/Test Dependencies (NOT for production)
# ------------------------------------------------------------------------------
# FILE ROLE:
#   Adds developer + test tooling on top of the production dependency set.
#   Production deployments should install ONLY:
#     - ai_service/requirements.txt
#
# USAGE:
#   Dev / CI (recommended):
#     pip install -r ai_service/requirements-dev.txt
#
#   Production (strict):
#     pip install -r ai_service/requirements.txt
# ==============================================================================

# Always include production deps first (keeps dev env consistent with prod)
-r requirements.txt


# ------------------------------------------------------------------------------
# Testing
# ------------------------------------------------------------------------------
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
fakeredis>=2.23,<3.0
hypothesis>=6.100,<7.0


# ------------------------------------------------------------------------------
# Linting / Formatting (fast, modern toolchain)
# ------------------------------------------------------------------------------
ruff>=0.6,<1.0


# ------------------------------------------------------------------------------
# Type checking (optional but recommended for master’s-level hygiene)
# ------------------------------------------------------------------------------
mypy>=1.10,<2.0
types-redis>=4.6,<5.0
types-requests>=2.32,<3.0


# ------------------------------------------------------------------------------
# Developer experience
# ------------------------------------------------------------------------------
ipython>=8.20,<9.0
